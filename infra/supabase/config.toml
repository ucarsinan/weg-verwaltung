# Supabase CLI config — local development stack.
# Replace project_id when linking to a remote project via `supabase link --project-ref <ref>`.

project_id = "weg-verwaltung-local"

[api]
enabled = true
port = 54321

[db]
port = 54322
major_version = 16

[studio]
enabled = true
port = 54323
