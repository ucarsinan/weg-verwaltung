# packages/shared-types

**Status:** Placeholder.

Will contain TypeScript types shared between `apps/web` and tooling — generated from the Supabase schema via `supabase gen types typescript`, plus hand-written domain types where database types are insufficient (e.g. discriminated unions for voting principles, agent suggestion shapes).
